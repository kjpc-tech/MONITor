<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="tech.kjpc.monitorapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service android:name="tech.kjpc.monitorapp.MONITorCheckerService"
            android:enabled="true">
        </service>

        <receiver android:name="tech.kjpc.monitorapp.MONITorBroadcastReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
        </receiver>

        <receiver android:name="tech.kjpc.monitorapp.MONITorAlarmReciever"
            android:enabled="true">
        </receiver>

        <activity android:name="tech.kjpc.monitorapp.MONITorMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="tech.kjpc.monitorapp.MONITorWebViewActivity"
                  android:label="@string/activity_monitor_webview_title"
                  android:theme="@style/AppTheme"
                  android:parentActivityName="tech.kjpc.monitorapp.MONITorMainActivity">
        </activity>
    </application>

</manifest>